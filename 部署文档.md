# 成长工作台 · PWA 多设备同步版 · 部署文档

## 项目简介
本工作台已按「路线 A」改造为**多设备同步的渐进式网页应用（PWA）**：

- 完全使用 **Supabase 云端数据库**持久化，**彻底删除浏览器 localStorage**（解决容量上限、设备隔离、清缓存丢数据三大问题）
- 标准 **PWA**：可安装到电脑 / 手机桌面，独立窗口运行、隐藏地址栏，支持基础离线访问
- 简易**账号密码登录**，仅本人可访问自己的数据
- 断网时的操作进入**本地队列**，恢复网络后**自动同步**云端
- 保留原有全部界面与业务逻辑（记账 / 健康 / 习惯打卡 / 英语 / 知识库 / 灵光池），并带**一键 JSON 导出备份**
- 最终产物为 **HTTPS 可访问的纯静态前端，无需自建后端服务器**

## 交付文件清单（即压缩包内文件）
| 文件 | 作用 |
|------|------|
| `index.html` | 主页面（全部界面与业务逻辑） |
| `app.js` | 数据层：Supabase 读写 / 登录门 / 离线队列 / PWA 注册 |
| `sw.js` | Service Worker：离线缓存应用壳 |
| `config.js` | **Supabase 密钥配置（部署前必改）** |
| `manifest.json` | PWA 清单（名称 / 图标 / 独立窗口） |
| `icon-192.png` `icon-512.png` `icon-maskable-512.png` `icon-180.png` `icon.svg` | 安装到桌面用的图标 |
| `部署文档.md` | 本文件 |
| `_transform.py` | （可选）从旧版单文件改造为本版的转换脚本，仅作参考，部署不需要 |

---

## ① Supabase：注册、建项目、获取密钥、建数据表

### 1. 注册并新建项目
1. 打开 https://supabase.com ，点右上角 **Sign Up**（可用 GitHub 或邮箱，免费）。
2. 登录后点 **New project**，填写：
   - **Name**：任意，如 `growth-workbench`
   - **Database Password**：记牢（平时一般不用）
   - **Region**：选离你近的，如 `Southeast Asia (Singapore)`
   - **Pricing Plan**：选 **Free**（免费额度足够个人使用）
3. 点 **Create new project**，等待约 1–2 分钟初始化完成。

### 2. 获取密钥
1. 进入项目，左侧最下方 **Settings → API**。
2. 复制两个值备用：
   - **Project URL**：形如 `https://xxxx.supabase.co`
   - **Publishable key**：以 `sb_publishable_` 开头的一串（这是**公开安全**的，靠 RLS 保护数据，可放在前端）
   - ⚠️ 新版 Supabase 已把旧版 `eyJ...` 的 anon key 改名为 **Publishable key**，功能完全一样，复制时务必点 🔑 展开**复制完整整串**，不要只复制下拉框里显示的前半段。
3. 这两个值稍后填进 `config.js`。

### 3. 关闭邮件确认（重要，否则注册后无法自动登录）
1. 左侧 **Authentication → Providers**。
2. 找到 **Email**，点开，把 **Confirm email** 开关**关掉**。
3. 保存。

### 4. 建数据表（运行 SQL）
1. 左侧 **SQL Editor → New query**。
2. 粘贴下面整段 SQL，点 **Run**：

```sql
-- 工作台数据存储表
create table if not exists public.wb_store (
  user_id    text        not null,
  store      text        not null,
  value      jsonb,
  updated_at timestamptz default now(),
  primary key (user_id, store)
);

-- 强制行级安全（连表 owner 也受 RLS 约束，更安全）
alter table public.wb_store force row level security;

grant all on table public.wb_store to anon, authenticated;

-- 行级安全：只允许登录用户读写自己 user_id 的数据
-- 注意：用 auth.jwt() ->> 'sub' 取用户 ID（text），与代码中 user_id(text) 类型一致
drop policy if exists "own_select" on public.wb_store;
create policy "own_select" on public.wb_store for select using (auth.jwt() ->> 'sub' = user_id);

drop policy if exists "own_insert" on public.wb_store;
create policy "own_insert" on public.wb_store for insert with check (auth.jwt() ->> 'sub' = user_id);

drop policy if exists "own_update" on public.wb_store;
create policy "own_update" on public.wb_store for update using (auth.jwt() ->> 'sub' = user_id) with check (auth.jwt() ->> 'sub' = user_id);

drop policy if exists "own_delete" on public.wb_store;
create policy "own_delete" on public.wb_store for delete using (auth.jwt() ->> 'sub' = user_id);
```

3. 运行无报错即完成。**表名 `wb_store`、列 `user_id` / `store` / `value` 必须与代码一致，请勿改名。**

---

## ② 密钥在项目内的配置位置
本项目 `config.js` **已预填 Max 的真实 Supabase 项目**（2026-08-12 直连验证通过），一般无需再改即可直接部署。

如需换到自己的项目，打开压缩包里的 **`config.js`**，把第 8–9 行换成你的项目值：

```js
window.SB_URL  = 'https://你的项目.supabase.co';                       // ← 改成你的 Project URL
window.SB_ANON = 'sb_publishable_你的完整publishable key';            // ← 改成你的 Publishable key
```

- 第 12 行 `window.SB_TABLE = 'wb_store';` 是表名，与上面 SQL 一致，**一般不用改**。
- 改完保存后，**必须重新部署一次**（静态文件改动需重新上传 / 重新发布才会生效）。

---

## ③ 静态网页部署（Vercel 或 GitHub Pages 二选一）
本项目是**纯静态前端，无需任何后端服务器**，部署后即为 HTTPS 可访问。

### 方式 A：Vercel（推荐，最简单）
**方法 1：网页拖拽上传**
1. 注册 https://vercel.com （建议用 GitHub 登录）。
2. 进入 Dashboard，把本压缩包解压后的**整个文件夹**直接拖到页面的上传区。
3. Framework Preset 选 **Other**，其余默认，点 **Deploy**。
4. 几十秒后获得 `https://xxx.vercel.app` 域名，部署完成。

**方法 2：连接 GitHub 自动部署**
1. 把文件夹内容 push 到一个 GitHub 仓库。
2. Vercel 导入该仓库，Framework 选 **Other**，点 Deploy。
3. 以后改 `config.js` 重新 push 即自动更新。

### 方式 B：GitHub Pages（免费，适合自定义域名）
1. 在 GitHub 新建一个**公开**仓库（如 `growth-workbench`）。
2. 把压缩包内所有文件上传到仓库根目录（可用网页 "Add file" 或 `git push`）。
3. 仓库 **Settings → Pages → Build and deployment → Source 选 "Deploy from a branch" → Branch 选 `main` / 目录 `/(root)` → Save**。
4. 约 1 分钟后访问 `https://你的用户名.github.io/仓库名/` 即可。
5. 说明：GitHub Pages 项目页 URL 带 `/仓库名/` 子路径，本项目 manifest 与 Service Worker 均使用相对路径，已兼容，无需额外改动。

---

## ④ PWA 安装到桌面（当 App 使用）
**前提：必须 HTTPS 访问**（Vercel / GitHub Pages 默认都是 HTTPS）。

- **电脑（Chrome / Edge）**：打开网址 → 地址栏右侧出现**安装图标**（⬇ 或显示器形状），点击即安装为独立窗口应用（无地址栏）。也可点浏览器右上角 ⋮ →「安装应用」/「将此站点另存为应用」。
- **安卓手机（Chrome）**：打开网址 → 右上角 ⋮ →「安装应用」/「添加到主屏幕」，桌面生成图标，点击即独立 App 打开。
- **iPhone / iPad（Safari）**：打开网址 → 点底部**分享**按钮 →「添加到主屏幕」→ 桌面生成图标。

---

## ⑤ 常见问题排查

- **打开白屏 / 一直停在登录页**
  - 确认访问的是 **HTTPS** 地址；
  - 确认 `config.js` 已填入真实 URL 与 anon key（不是 `YOUR-...` 占位符）；
  - 确认 Supabase 表 `wb_store` 已建、RLS 已开启、四条 policy 已建。

- **注册 / 登录失败**
  - 密码至少 6 位；
  - 若注册后无法自动登录，回到 ①-3 确认已**关闭 Confirm email**；或先「注册」再「登录」。

- **数据在多设备不同步**
  - Supabase 后台 SQL Editor 跑 `select * from wb_store;` 看是否有数据写入；
  - 浏览器按 F12 → Network，看 `/rest/v1/wb_store` 请求是否 200；401 / 403 多为 RLS 策略或 anon key 错误；
  - 确认每次改动后已重新部署。

- **无法「安装」为 App**
  - 必须在 HTTPS 下；manifest 中 `display: standalone`、icons 齐全（本项目已满足）；Service Worker 需注册成功（首次需联网加载一次以缓存）。

- **离线后打不开 / 数据不显示**
  - PWA 离线靠 SW 缓存应用壳，首次必须联网打开过一次以完成缓存；
  - 离线期间的增删改会进入本地队列，恢复网络后自动同步（右上角徽标显示「同步中 → 已同步」）。

- **想清空全部数据**
  - 工作台内「数据备份」区有清空按钮（调用云端清空），操作前建议先「导出 JSON 备份」。

---

> 数据默认实时存云端，换手机 / 电脑登录同一账号即可延续。仍建议每周点一次「导出 JSON 备份」作为额外保险。
